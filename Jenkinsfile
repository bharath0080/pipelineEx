@Library("sample-library-script") _
pipelineShared {
	slave="Slave name on which job needs to be executed"
	Git_url="GIT url"
	Git_Credentials="credentials ID"
	Branch_Name="branch name"
	Maven_goal="package/release"
	code_coverage_tool= "jacoco/cobertura"
	email = "emailID to which mail should send if failed"
}
